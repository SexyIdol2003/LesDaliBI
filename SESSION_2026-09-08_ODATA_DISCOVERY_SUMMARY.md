# Итог OData-разведки 1С:ERP АПК — 08.09.2026

## Цель

Найти реальные OData-источники 1С:ERP Агропромышленный комплекс для будущих BI-витрин:

1. План–факт расхода ГСМ, л/га, по ключевым операциям: вспашка, гребнеобразование, опрыскивание.
2. Фактический расход ГСМ на 1 га в разрезе техники.
3. Расход удобрений и СЗР в рублях на 1 га по сезонам.
4. Факт сбора урожая: поле, сорт, т/га.
5. Фактические затраты в рублях на каждую единицу техники за период.
6. Фактическая себестоимость 1 га в разрезе сезонов.

## Главный способ поиска метаданных

В этой базе корневой OData `$metadata` падает с ошибкой `odata.error code -1` («Внутренняя ошибка OData сервиса»), поэтому нельзя полагаться на получение полного каталога сущностей.

Надёжный способ узнать техническое имя объекта без доступа к конфигуратору:

1. Открыть список/карточку объекта в клиенте 1С.
2. Выбрать «Все действия» → «Изменить форму…».
3. В пути формы взять часть между `Документ.` (или `РегистрНакопления.`) и `.Форма`.
4. Использовать её в OData как `Document_<имя>` или `AccumulationRegister_<имя>`.

Примеры найденных путей:

- `Документ.ДвижениеПродукцииИМатериалов.Форма.ФормаДокумента`
- `Документ.АпкПутевойЛистТракториста.Форма.ФормаДокумента`
- `Документ.АпкВыполнениеРемонтаТС.Форма.ФормаДокумента`

## Рабочие OData-сущности

| Назначение | OData-сущность | Проверка |
|---|---|---|
| Универсальный документ движения продукции и материалов | `Document_ДвижениеПродукцииИМатериалов` | 200 |
| Фактические материалы растениеводства | `AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту` | 200 |
| Путевой лист тракториста | `Document_АпкПутевойЛистТракториста` | 200 |
| Путевой лист автотранспорта | `Document_АпкПутевойЛистАвтотранспорта` | 200 |
| Заправочные ведомости | `Document_АпкЗаправочныеВедомости` | 200 |
| Списание топлива по суммарной заправке | `Document_АпкСписаниеТопливаПоСуммарнойЗаправке` | 200 |
| Выполнение ремонта ТС | `Document_АпкВыполнениеРемонтаТС` | 200 |
| Запасы и потребности | `AccumulationRegister_ЗапасыИПотребности` | 200 |
| Товары на складах | `AccumulationRegister_ТоварыНаСкладах` | 200 |
| Себестоимость товаров | `AccumulationRegister_СебестоимостьТоваров` | 200, возможны долгие ответы |

## 1. Акты списания семян, удобрений и ядов

### Техническая модель

Пользовательский документ «Акт списания семян, удобрений и ядов» не является отдельной OData-сущностью. Он реализован как разновидность универсального документа:

```text
Document_ДвижениеПродукцииИМатериалов
```

Признак нужного вида документа:

```text
АпкВидДокумента = 'АктНаСписанияСемянУдобренийИЯдов'
```

Проверенный запрос:

```bash
curl -s -u 'OdataBi:***' \
  "http://10.50.254.22/ld_erp_ibiOdata/odata/standard.odata/Document_ДвижениеПродукцииИМатериалов?\$format=json&\$filter=АпкВидДокумента%20eq%20'АктНаСписанияСемянУдобренийИЯдов'&\$top=1000"
```

### Поля заголовка

- `Ref_Key`, `Number`, `Date`, `Posted`, `Статус`
- `Организация_Key`
- `Отправитель`, `Получатель` — ссылки на склады
- `ХозяйственнаяОперация`
- `АпкВидДокумента`
- `АпкВидРаботы_Key`
- `АпкОбъектЗатрат_Key`

### Табличная часть `Товары`

- `Номенклатура_Key`, `Характеристика_Key`, `Серия_Key`
- `Количество`, `КоличествоУпаковок`
- `АпкПлощадьОбработанная`
- `АпкРасходНаГа`
- `ГруппаПродукции_Key`
- `Цена`, `Сумма` — в проверенных документах равны 0, поэтому не использовать как источник рублёвой оценки.

### Прямой вывод

Документ даёт факт списания материалов, площадь обработки и норму/факт количества на гектар. Для рублевой оценки нужны регистры себестоимости либо другой источник стоимостей.

## 2. Материалы в растениеводстве по факту

Рабочий регистр:

```text
AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту
```

Проверенные поля набора записей:

- `Period`
- `Организация_Key`
- `ВидРаботы_Key`
- `Номенклатура_Key`
- `ОбъектЗатрат_Key`
- `Склад_Key`
- `РасходНаГа`
- `Количество`
- `ПлощадьОбработанная`

Пример подтверждённой строки: `Количество = 64.038`, `ПлощадьОбработанная = 100`, `РасходНаГа = 0.64`.

Регистратором записи является `Document_ДвижениеПродукцииИМатериалов` с признаком:

```text
АпкВидДокумента = АктНаСписанияСемянУдобренийИЯдов
```

В этом документе нет `Техника_Key`. Значит, регистр даёт фактические материалы по операции/полю/площади, но не даёт прямую привязку к конкретному трактору.

## 3. Путевой лист тракториста

Рабочая сущность:

```text
Document_АпкПутевойЛистТракториста
```

### Шапка

- `Техника_Key` — конкретная единица техники.
- `Механизатор` — физическое лицо.
- `МоточасовПриВыезде`, `МоточасовПриВозвращении`, `МоточасовВсего`.
- `ДатаНачалаРабот`, `ДатаОкончанияРабот`.

### Табличная часть `ВыполненныеРаботы`

- `ВидРаботы_Key`.
- `Гектаров`.
- `НормативныйРасходГСМ`.
- `НормаРасходаГСМ`.
- `Часов`, `ЧасовПоНорме`, `Моточасов`.
- `ИтогоЗП`.
- `АналитикаРасходов`.

Проверка 15 документов показала множество ненулевых значений `Гектаров` и `НормативныйРасходГСМ`. Значит, это корректный источник плановой/нормативной части для разреза техника + работа + гектары.

### Ограничение

В проверенной выборке `ФактическийРасходГСМ` был нулевым, а табличная часть `ГСМ` могла быть пустой. Не использовать путевой лист тракториста как самостоятельный источник факта ГСМ.

## 4. Путевой лист автотранспорта

Рабочая сущность:

```text
Document_АпкПутевойЛистАвтотранспорта
```

Это источник для автотранспорта, не основной для полевых тракторных операций.

В табличной части `ГСМ` доступны:

- `ФактическийРасходГСМ`
- `НормативныйРасходГСМ`
- `ОтклонениеОтНормы`

В `ВыполненныеРаботы` доступны `ВидРаботы_Key`, `Часов`, `ЧасовПоНорме`, `ИтогоЗП`, `АналитикаРасходов`.

Итог: для автотранспорта план-факт уже хранится напрямую в путевом листе.

## 5. Заправочные ведомости

Рабочая сущность:

```text
Document_АпкЗаправочныеВедомости
```

### Поля

Шапка:

- `ТранспортноеСредство_Key`
- `Склад_Key`
- `Организация_Key`

Табличная часть `ГСМ`:

- `Дата`
- `МаркаТоплива_Key`
- `Количество`
- `ТранспортноеСредство_Key` (может быть пустым, когда техника задана в шапке)

Это источник выдачи/заправки топлива. Сам по себе не связывает топливо с конкретной полевой операцией и полем.

## 6. Списание топлива по суммарной заправке

Рабочая сущность:

```text
Document_АпкСписаниеТопливаПоСуммарнойЗаправке
```

Это ключевой источник фактического расхода топлива по технике за период.

### Шапка

- `НачалоПериода`
- `ОкончаниеПериода`
- `Организация_Key`
- `Подразделение_Key`

### Табличная часть `ГСМ`

- `ТранспортноеСредство_Key`
- `МаркаТоплива_Key`
- `НачальныйОстаток`
- `КонечныйОстаток`
- `Заправлено`
- `ФактическийРасход`

Подтверждено на месячных документах: фактический расход рассчитывается как расход за период по каждой единице техники. Например, при `Заправлено = 1189`, `НачальныйОстаток = 0`, `КонечныйОстаток = 0` получен `ФактическийРасход = 1189`; в другом примере при начальном остатке 102 и заправке 104 — расход 206.

### Ограничение

Документ агрегирован за период и не содержит непосредственной разбивки на поле и вид работы. Его нужно использовать как факт ГСМ на технику за месяц/период, а операционную детализацию получать из путевых листов и/или регистра материалов растениеводства.

## 7. Выполнение ремонтов ТС

Рабочая сущность:

```text
Document_АпкВыполнениеРемонтаТС
```

### Поля шапки

- `ТранспортноеСредство_Key`
- `ОбъектЭксплуатации_Key`
- `ДатаНачалаФактическая`
- `ДатаЗавершенияФактическая`
- `Статус`
- `ЗаказНаРемонт_Key`

### Табличные части

`Ремонты`:

- `Узел_Key`, `ВидРемонта_Key`, `ОписаниеРемонта`, `ПричинаРемонта`.

`ВыполненныеРаботы`:

- `ВидРаботы_Key`
- `Количество`, `Расценка`, `Часов`, `ЧасовПоНорме`
- `ОсновнаяЗП`, `ДополнительнаяЗП`, `ИтогоЗП`
- `Исполнитель`

`МатериалыИРаботы`, `МатериалыВнешние`, `ВыполненныеРаботыВнешние` также присутствуют; в проверенном документе они были пустыми.

### Важный фильтр

Первый пример был помечен `DeletionMark = true` и `Posted = false`. В BI обязательно исключать такие документы, по крайней мере:

```text
DeletionMark eq false and Posted eq true
```

Итог: источник покрывает трудозатраты/зарплату ремонта по технике; для полной стоимости ремонта нужно проверить наполненность материальных и внешних табличных частей на проведённых документах.

## 8. Себестоимость и стоимость материалов

### Что уже известно

В движениях акта списания видны регистры:

- «Себестоимость материалов растениеводства»
- «Себестоимость товаров»
- «Себестоимость материалов растениеводства» содержит поля, связанные с организацией, объектом затрат, складом, номенклатурой, количеством и стоимостью регламентированного учёта.

### Что подтверждено OData

```text
AccumulationRegister_СебестоимостьТоваров
```

отвечает с HTTP 200, но может работать заметно медленнее обычных регистров.

### Что пока не найдено

Техническое имя регистра, отображаемого как «Себестоимость материалов растениеводства», не установлено. Попытки:

- `AccumulationRegister_СебестоимостьМатериаловРастениеводства`
- `AccumulationRegister_АпкСебестоимостьМатериаловРастениеводства`
- `AccumulationRegister_АпкСебестоимостьРастениеводства`
- `AccumulationRegister_АпкСебестоимостьПродукцииРастениеводства`
- `AccumulationRegister_АпкРасчетСебестоимостиРастениеводства`
- `AccumulationRegister_АпкСебестоимостьМатериалов`

вернули 404.

### Следующее действие

В 1С найти регистр через движения документа «Акт списания семян, удобрений и ядов» → «Себестоимость материалов растениеводства» → открыть список/форму → «Все действия» → «Изменить форму…» → получить путь `РегистрНакопления.<Имя>.Форма...`.

Дополнительно в поиске 1С по «себестоимость» найден «Отчет "Себестоимость материальных затрат"» — его нужно открыть, не изменяя данных, и проверить настройки/техническое имя; он может привести к нужному регистру.

## Итог по BI-метрикам

| Метрика | Источники | Состояние |
|---|---|---|
| План-факт ГСМ, л/га, по операциям | План: `Document_АпкПутевойЛистТракториста.ВыполненныеРаботы`; факт материалов: `AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту`; факт на технику за период: `Document_АпкСписаниеТопливаПоСуммарнойЗаправке` | Источники найдены; для прямой строки техника+операция+факт л/га потребуется метод распределения/сопоставления |
| Факт ГСМ на 1 га по технике | `Document_АпкСписаниеТопливаПоСуммарнойЗаправке` + путевые листы тракториста | Частично: месячный факт по технике и операционные гектары доступны, но нет прямого ключа факт-операция |
| Удобрения и СЗР, количество на 1 га | `Document_ДвижениеПродукцииИМатериалов` с `АпкВидДокумента`; `AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту` | Закрыто для количества/га |
| Удобрения и СЗР, рубли на 1 га | Те же источники + неустановленный регистр себестоимости | Не закрыто: нужен источник стоимостей |
| Урожай: поле, сорт, т/га | В проектной архитектуре предусмотрены `raw.r1c_harvest_doc`, `raw.r1c_harvest_lines` | Требуется отдельно подтвердить фактический OData-источник и поля |
| Рубли на технику за период | ГСМ: `Document_АпкСписаниеТопливаПоСуммарнойЗаправке` (литры); труд ремонта: `Document_АпкВыполнениеРемонтаТС.ВыполненныеРаботы.ИтогоЗП`; эксплуатационная зарплата: путевые листы | Частично: нужна цена ГСМ и стоимость запчастей/внешних работ |
| Себестоимость 1 га за сезон | Материалы по факту + стоимостной регистр + другие затраты | Не закрыто: требуется техническое имя регистра себестоимости и модель распределения затрат |

## Рекомендованные следующие шаги

1. Получить техническое имя регистра «Себестоимость материалов растениеводства» через форму регистра в клиенте 1С.
2. Проверить `МатериалыИРаботы`, `МатериалыВнешние`, `ВыполненныеРаботыВнешние` у проведённых и не помеченных на удаление документов `АпкВыполнениеРемонтаТС`.
3. Найти документ «Заказы на ремонт ТС» через «Изменить форму…» при необходимости план-факт ремонтов; для фактических затрат он не является блокирующим источником.
4. Найти и подтвердить OData-источник уборки урожая, включая поле, сорт, массу и площадь.
5. Получить справочники для расшифровки ключей: техника, номенклатура, виды работ, поля/объекты затрат, культуры и сорта.
6. После подтверждения источников реализовать raw-загрузки с пагинацией, затем staging-модели и витрины `mart.fact_fuel_usage`, `mart.fact_tmc_usage`, `mart.fact_field_costs`, `mart.fact_harvest`.
