# Находка: OData-источники для ГСМ (план-факт), себестоимости и затрат на технику

Дата фиксации: 2026-09-08
Репозиторий: https://github.com/SexyIdol2003/LesDaliBI

## Контекст

Продолжение сессии SESSION_2026-09-08_FIELD_WRITEOFF_ODATA_DISCOVERY.md.
Задача: найти OData-источники для 6 BI-метрик:
1. План-факт ГСМ (л/га) по операциям (вспашка, гребнеобразование, опрыскивание).
2. Факт расхода ГСМ на 1 га по технике (например: К701 — вспашка — 5 л/га).
3. Расход руб. удобрений и СЗР на 1 га по сезонам.
4. Факт сбора урожая: поле — сорт — тн/га.
5. Факт руб. на каждую технику за период.
6. Факт себестоимость 1 га за сезон.

## Найденные объекты OData

### Document_АпкПутевойЛистТракториста
Найден через клиент 1С: раздел "Растениеводство" → "Все функции" →
"Механизированные работы" → "Путевые/учетные листы тракториста" →
"Все действия" → "Изменить форму..." → путь
`Документ.АпкПутевойЛистТракториста.Форма.ФормаДокумента`.

Ключевые поля:
- Шапка: `Техника_Key` (конкретная единица техники), `Механизатор`,
  `МоточасовВсего`.
- Табличная часть `ВыполненныеРаботы`: `ВидРаботы_Key` (вспашка/опрыскивание/
  гребнеобразование), `Гектаров`, `НормативныйРасходГСМ`, `НормаРасходаГСМ`,
  `Часов`, `ЧасовПоНорме`, `ИтогоЗП`, `АналитикаРасходов` (Catalog_СтруктураПредприятия).
- Табличная часть `ГСМ`: должна содержать факт расхода, но в проверенных
  15 записях `ФактическийРасходГСМ` = 0 во всех случаях — факт расхода почти
  никогда не проставляется прямо в этом документе.

Подтверждено (top=15): `Гектаров` и `НормативныйРасходГСМ` содержат много
ненулевых значений — план по ГСМ на операцию+технику+га реально ведётся здесь.

### Document_АпкПутевойЛистАвтотранспорта
Аналог для автотранспорта (легковые/грузовые, не полевая техника).
Табличная часть `ГСМ` содержит `ФактическийРасходГСМ`, `НормативныйРасходГСМ`,
`ОтклонениеОтНормы` — готовая план-факт структура. Табличная часть
`ВыполненныеРаботы` содержит `ВидРаботы_Key`, `АналитикаРасходов`, зарплату.

### Document_АпкЗаправочныеВедомости
Заправочные ведомости — фиксируют выдачу топлива на конкретную технику
(`ТранспортноеСредство_Key` в шапке) по датам, табличная часть `ГСМ`:
`МаркаТоплива_Key`, `Количество`. Это скорее "приход" топлива в бак техники,
не привязан к конкретной операции/полю.

### AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту
Регистратор — `Document_ДвижениеПродукцииИМатериалов` (универсальный документ).
Поля: `ВидРаботы_Key`, `Номенклатура_Key`, `ОбъектЗатрат_Key` (поле),
`РасходНаГа` (готовая метрика расхода на гектар — количество, не рубли),
`Количество`, `ПлощадьОбработанная`. Не содержит явного `Техника_Key` —
хотя может быть общим фактом расхода материалов (топливо, удобрения, СЗР)
на га по виду работы и полю, без привязки к конкретной единице техники.

### AccumulationRegister_ЗапасыИПотребности, AccumulationRegister_ТоварыНаСкладах
Стандартные регистры ERP, технические имена совпадают с русским названием
без пробелов (правило работает для НЕ-АПК регистров). Полезны как побочное
подтверждение общей механики именования, не основной источник для BI-задачи.

## Правило именования (важное открытие для будущих поисков)

Для АПК-специфичных объектов (документов и регистров) техническое имя
получается из русского названия без пробелов **с добавлением приставки
`Апк` в начале**, например:
- «Заправочные ведомости» → `АпкЗаправочныеВедомости`
- «Путевой лист автотранспорта» → `АпкПутевойЛистАвтотранспорта`
- «Путевые/учетные листы тракториста» → `АпкПутевойЛистТракториста`
- «Материалы в растениеводстве по факту» → `АпкМатериалыВРастениеводствеПоФакту`

Для стандартных (не отраслевых) объектов ERP приставка не нужна:
- «Запасы и потребности» → `ЗапасыИПотребности`
- «Товары на складах» → `ТоварыНаСкладах`

Но угадывание имени вслепую по-прежнему ненадёжно (несколько попыток дали
404 даже с правильным шаблоном) — самый быстрый гарантированный способ:
открыть объект в клиенте 1С → "Все действия" → "Изменить форму..." → взять
имя из пути `Документ.ИмяДокумента.Форма...` или
`РегистрНакопления.ИмяРегистра.Форма...`.

## Статус по всем 6 метрикам

| # | Метрика | Источник | Статус |
|---|---|---|---|
| 1 | План-факт ГСМ по операциям (л/га) | `Document_АпкПутевойЛистТракториста` (план в `ВыполненныеРаботы`), факт вероятно в `AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту` | Источники найдены, факт по технике не подтверждён напрямую |
| 2 | Факт ГСМ на 1 га по технике | `Техника_Key` (шапка путевого листа) + `Гектаров`/`НормативныйРасходГСМ` (план); факт требует связки с регистром материалов | Частично — нет прямой связи техника+факт в одном объекте |
| 3 | Руб. удобрений/СЗР на 1 га по сезонам | `Document_ДвижениеПродукцииИМатериалов` (`АпкВидДокумента='АктНаСписанияСемянУдобренийИЯдов'`) + `AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту` | Количество/га есть; рубли требуют регистра себестоимости (не найден, `Цена`/`Сумма` в документе = 0) |
| 4 | Урожайность поле-сорт-т/га | `raw.r1c_harvest_doc`/`raw.r1c_harvest_lines` (уже в архитектуре DWH) | Предположительно закрыт в более ранней сессии |
| 5 | Руб. на технику за период | `Document_АпкПутевойЛистТракториста.ВыполненныеРаботы.ИтогоЗП` (зарплата механизатора) + возможно "Выполнение ремонтов ТС" (не проверен) | Частично — есть зарплата, нет ГСМ+ремонты в рублях |
| 6 | Себестоимость 1 га за сезон | `mart.fact_field_costs` (спроектирован в DWH) | Модель спроектирована, реальный расчёт не подтверждён |

## Следующие шаги

1. Проверить документ, на который ссылается `Recorder` в
   `AccumulationRegister_АпкМатериалыВРастениеводствеПоФакту`, — возможно,
   там (в самом `Document_ДвижениеПродукцииИМатериалов` с другим
   `АпкВидДокумента`) есть поле техники, которое регистр не показывает.
2. Проверить документ "Списания топлива по суммарной заправке" (виден в
   меню "Учет ГСМ") — вероятный источник факта расхода, привязанного к
   операции/технике.
3. Проверить "Выполнение ремонтов ТС" и "Заказы на ремонт ТС" для полных
   затрат на технику в рублях.
4. Найти регистр себестоимости растениеводства для перевода количества
   удобрений/СЗР в рубли на гектар (пока не найден под этим именем; см.
   предыдущий файл SESSION_2026-09-08_FIELD_WRITEOFF_ODATA_DISCOVERY.md).
