# Сессия 2026-09-08 — найдена площадь поля в Catalog_АпкПоля

Репозиторий: https://github.com/SexyIdol2003/LesDaliBI

## Контекст

С 2026-08-19 было известно, что `raw.r1c_polya.area_ha` пусто во всех 48 полях — это блокировало расчёт урожайности (т/га) и себестоимости на гектар. Раньше (STATUS_2026-08-13) `Catalog_АпкПоля` вообще возвращал `odata.error code:20 "Доступ запрещён"`.

## Что обнаружено сегодня

1. **Права на `Catalog_АпкПоля` открыты** — сущность отвечает нормально (нет ни 404, ни 20/"Доступ запрещён"), в отличие от состояния на 13 августа.
2. **Площадь поля не существует как плоское поле** — предположение `АпкПлощадьПоляГа` из старого DAG `dag_extract_catalogs.py` было неверным (404 при явном select).
3. **Реальная структура**: площадь хранится в табличной части `ИсторияПоля` — по одной строке на год урожая/культуру на поле. Ключевые поля строки:
   - `ГодУрожая` — год
   - `Культура` / `Культура_Type` — ссылка на `Catalog_АпкКультуры`
   - `ПлощадьОбщая` — общая площадь, га (то, что нужно для т/га и себестоимости)
   - `ПлощадьСева`, `ПлощадьКонтура` — площадь сева / контура (часто 0)
   - `НачалоПериодаАктуальности` / `КонецПериодаАктуальности` — период действия строки
   - `Предшественник_Key` / `ПредшественникПредставление` (часто "Пар")
   - `Подразделение_Key`, `ОбъектыЗатратПредставление` (человекочитаемая сводка вида "318, Пар 2020 г. (не исп. с 31.12.2020)")

Проверено на поле №318 (`Ref_Key d7ca999f-f153-11eb-9c3a-00505689ff45`): история содержит записи с 2020 по 2026 год, площади от 1.2 до 465 га на разных контурах/культурах в разные периоды.

## Сделано

- Новый DAG `files/airflow/dags/dag_extract_polya_istoriya.py` — грузит `Catalog_АпкПоля` с `$expand=ИсторияПоля` в две raw-таблицы: `raw.r1c_polya` (справочник) и `raw.r1c_polya_istoriya` (история площадей по годам).
- SQL `files/20_raw_polya_istoriya.sql` — расширяет `raw.r1c_polya` реальными полями (склады семян/удобрений/СЗР/продукции, ЕФИС-номер, организация), создаёт `raw.r1c_polya_istoriya` и витрину `mart.v_field_area_by_season` для соединения площади с фактом урожая по сезону.

## Открытые вопросы для следующей сессии

1. Прогнать DAG `dag_extract_polya_istoriya` и подтвердить реальное число строк (`fields_count`, `history_count` в логе `quality_check`).
2. Связать `mart.fact_harvest` с `mart.v_field_area_by_season` по `field_sk` + году сбора, чтобы посчитать `yield_t_ha` (сейчас `area_ha` в `fact_harvest` не заполнена).
3. Уточнить у агронома соответствие `ГодУрожая` в 1С и `season`/`season_label` в `mart.dim_date` (сезон считается с 1 октября — нужно проверить, что `ГодУрожая` использует ту же границу).
4. Аналогично проверить, не возвращает ли теперь `Catalog_АпкМоделиСХТехники` данные (тоже был заблокирован 20/"Доступ запрещён" вместе с полями 13 августа).
