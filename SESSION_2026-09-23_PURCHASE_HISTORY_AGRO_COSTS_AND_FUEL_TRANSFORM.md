# Сессия 2026-09-23: история закупок, денежная модель агровходов и автозагрузка контрагентов

Дата: 2026-09-23  
Репозиторий: `SexyIdol2003/LesDaliBI`  
Ветка: `main`  
Рабочий клон: `/Users/sexyidol2003/Desktop/ЛД БД/LesDaliBI/LesDaliBI`

## Цель

В сессии создан новый регулярный контур данных по закупкам 1С и денежная модель агровходов.

Цели:

1. Убрать ограничение старой модели закупочных цен только 2026 годом.
2. Загружать документы «Приобретение товаров и услуг» автоматически и за всю историю.
3. Загружать справочник контрагентов для аналитики закупок по поставщику.
4. Считать закупочные цены и стоимость агровходов по полям/месяцам.
5. Добавить семена в денежную модель наряду с СЗР и удобрениями.
6. Закрепить регулярное обновление факта списания ГСМ.
7. Выявить ограничения качества справочника номенклатуры.

## Файлы

### SQL

| Файл | Назначение |
|---|---|
| `files/36_raw_purchases.sql` | Year-agnostic raw-таблицы шапок и строк закупок |
| `files/37_mart_agro_input_purchase_price_and_cost_month.sql` | Базовая историческая витрина закупочных цен и стоимости агровходов |
| `files/37b_fix_v_agro_input_cost_per_field_month.sql` | Исправление витрины стоимости на поле/месяц |
| `files/38_add_semena_to_agro_input_cost_model.sql` | Категория «Семена» в модели цен и затрат |
| `files/39_raw_contractors_and_purchase_contract.sql` | Контрагенты, будущая ссылка на договор и витрина закупок по поставщику |

### Airflow

| Файл | Назначение |
|---|---|
| `files/airflow/dags/dag_extract_purchases.py` | Ежедневная загрузка документов закупки из 1С OData |
| `files/airflow/dags/dag_extract_contractors.py` | Ежедневная загрузка справочника контрагентов |
| `files/airflow/dags/dag_transform_mart.py` | Регулярная загрузка факта списания ГСМ |

### Backfill-утилиты

| Файл | Назначение |
|---|---|
| `export_purchase_headers_history.py` | Историческая выгрузка шапок закупок 2021–2025 в JSONL |
| `export_purchase_lines_history.py` | Историческая выгрузка строк закупок по годам с resume через `.progress` |

## Raw-слой закупок

Созданы единые таблицы без суффикса года:

```text
raw.r1c_purchase_headers
raw.r1c_purchase_lines
```

`raw.r1c_purchase_headers` хранит ключ документа, признак удаления/проведения, номер, дату, организацию, контрагента, признак цены с НДС, будущую ссылку на договор и `_loaded_at`.

`raw.r1c_purchase_lines` хранит ключ документа и строки, номенклатуру, характеристику, серию, количество, цену, сумму без НДС, ставку и сумму НДС, сумму с НДС, склад и `_loaded_at`.

Ключи:

```text
r1c_purchase_headers: _id
r1c_purchase_lines:   (doc_id, line_number)
```

Созданы индексы по дате документа, `nomenklatura_id` и `doc_id`. Загрузка идемпотентна через `INSERT ... ON CONFLICT DO UPDATE`.

## DAG закупок

Создан DAG:

```text
dag_extract_purchases
```

Источник:

```text
Document_ПриобретениеТоваровУслуг
```

Расписание:

```text
50 2 * * *
```

Цепочка:

```text
verify_entity
→ extract_purchases
→ [load_purchase_headers, load_purchase_lines]
→ quality_check
```

Из документа извлекаются шапка и вложенная табличная часть `Товары`.

Особенности этой инсталляции 1С OData:

- системные параметры должны содержать литеральный `$`; `%24` приводит к HTTP 500;
- для получения вложенного массива `Товары` нельзя передавать `$select`, иначе 1С возвращает HTTP 500;
- запросы собираются вручную, применяется пагинация `$top/$skip`;
- используются 4 HTTP-попытки для 500/502/503/504, timeout и ошибок соединения с backoff 2/4/8 секунд;
- на уровне Airflow включены 2 повтора с паузой 10 минут.

## DAG контрагентов

Создан DAG:

```text
dag_extract_contractors
```

Источник:

```text
Catalog_Контрагенты
```

Расписание:

```text
0 3 * * *
```

Загружаются:

```text
Ref_Key
DeletionMark
Description
ИНН
КПП
ЮридическоеФизическоеЛицо
```

Данные пишутся в:

```text
raw.r1c_contractors
```

Цепочка:

```text
verify_entity
→ extract_contractors
→ load_contractors
→ quality_check
```

Для справочника создан индекс по `description`, загрузка идемпотентна по `_id`.

## Результат исторической загрузки

После первого полного прогона закупок в PostgreSQL:

| Показатель | Значение |
|---|---:|
| Шапки документов закупки | 11 851 |
| Строки закупок | 25 847 |
| Первая дата | 2021-01-01 |
| Последняя дата | 2026-09-22 |
| Покрытие истории | 2021–2026 |

После запуска загрузки контрагентов:

| Показатель | Значение |
|---|---:|
| Контрагентов | 1 473 |
| С ИНН | 1 448 |
| С КПП | 1 034 |
| Последняя загрузка | 2026-09-23 15:54:47 |

Ручной run `dag_extract_contractors` завершился со статусом `success` за 6,8 секунды.

## Закупочные цены агровходов

Создана витрина:

```text
mart.v_agro_input_purchase_price
```

Источники:

```text
raw.r1c_purchase_lines
JOIN raw.r1c_purchase_headers
JOIN raw.r1c_nomenclature
```

Включены категории:

```text
СЗР
Удобрение
Семена
```

Фильтр модели:

```sql
WHERE n."АпкНазначение" = ANY (
    ARRAY['СЗР', 'Удобрение', 'Семена']
)
```

Effective price без НДС:

```sql
CASE
    WHEN kolichestvo > 0
        THEN ROUND(summa / kolichestvo, 6)
    ELSE NULL
END
```

Витрина содержит контрольные показатели: расхождение между суммой и `количество × цена`, процент расхождения, проверку классификации аммиачной селитры и статус качества данных.

## Исторические закупки СЗР и удобрений

| Год | Категория | Строк | Сумма без НДС, ₽ |
|---:|---|---:|---:|
| 2021 | СЗР | 19 | 1 433 700,20 |
| 2021 | Удобрение | 9 | 5 612 412,49 |
| 2022 | СЗР | 33 | 16 642 790,35 |
| 2022 | Удобрение | 19 | 12 480 275,01 |
| 2023 | СЗР | 61 | 20 363 194,28 |
| 2023 | Удобрение | 42 | 19 645 667,37 |
| 2024 | СЗР | 43 | 10 170 463,18 |
| 2024 | Удобрение | 24 | 11 091 849,99 |
| 2025 | СЗР | 34 | 14 918 372,73 |
| 2025 | Удобрение | 31 | 17 500 193,32 |
| 2026 | СЗР | 51 | 28 245 473,85 |
| 2026 | Удобрение | 38 | 29 093 486,89 |

Результат подтверждает, что закупочная модель теперь работает за всю историю 2021–2026, а не только за 2026 год.

## Семена

Файл `38_add_semena_to_agro_input_cost_model.sql` добавил семена в:

```text
mart.v_agro_input_purchase_price
mart.v_agro_input_cost_per_field_month
```

После применения SQL:

| Год | Категория | Строк | Сумма без НДС, ₽ |
|---:|---|---:|---:|
| 2022 | Семена | 1 | 180 000,00 |

SQL-модель семян работает, но результат явно неполон из-за отсутствующих записей в `raw.r1c_nomenclature`.

## Стоимость агровходов на поле

Создана финальная витрина:

```text
mart.v_agro_input_cost_per_field_month
```

Цепочка:

```text
списание материалов на поле
→ закупочная цена той же номенклатуры
→ объект затрат АПК
→ поле
→ площадь поля в сезоне
```

Приоритет цены:

1. Ближайшая валидная закупочная цена на дату списания или раньше.
2. Если такой цены нет — ближайшая будущая цена с маркировкой `ЦЕНА ПОСЛЕ ДАТЫ СПИСАНИЯ`.

В денежный расчёт попадают только строки со статусом `OK`.

Формулы:

```sql
material_cost_no_vat =
quantity * effective_price_no_vat
```

```sql
cost_rub_per_field_ha =
total_cost_rub_no_vat / field_area_ha
```

## Исправление SQL-ошибки

Первичный файл `37_mart_agro_input_purchase_price_and_cost_month.sql` создал витрину закупочных цен, но остановился при создании второй витрины с ошибкой:

```text
column "n.Description" must appear in the GROUP BY clause
```

Причина — лишнее поле `category_placeholder`.

Файл `37b_fix_v_agro_input_cost_per_field_month.sql`:

- удалил `category_placeholder`;
- удалил дублирующий join к номенклатуре;
- оставил единственный расчёт категории.

После этого `37b` и `38` успешно применены.

## Витрина поставщиков

Создана витрина:

```text
mart.v_purchase_summary_by_contractor
```

Поля:

```text
contractor_name
year
documents
lines
total_summa
```

Она позволяет контролировать закупки по поставщикам независимо от полноты классификации номенклатуры.

Примеры крупных закупок:

| Контрагент | Год | Сумма, ₽ |
|---|---:|---:|
| ЛИДЕР-ПАК АГРО ООО | 2024 | 131 763 361,91 |
| ЛИДЕР-ПАК АГРО ООО | 2023 | 73 218 059,35 |
| ЛИДЕР-ПАК АГРО ООО | 2025 | 72 202 889,68 |
| АзотХим | 2026 | 17 041 200,00 |
| АзотХим | 2023 | 16 363 500,05 |
| АВГУСТ АО ФИРМА | 2023 | 15 487 963,58 |
| ФЮЭЛТРАНСГРУПП ООО | 2023 | 11 339 125,32 |
| ФЮЭЛТРАНСГРУПП ООО | 2025 | 10 162 444,60 |
| ДГТ (Дока-Генные Технологии) ООО | 2025 | 11 090 909,05 |
| СЕЛЕКЦИЯ БУДУЩЕГО ООО | 2026 | 12 218 000,00 |

## Аудит покрытия номенклатурой

### СЗР и удобрения

| Контрагент | Строк | С найденной номенклатурой | Покрытие |
|---|---:|---:|---:|
| АзотХим | 110 | 100 | 91% |
| АВГУСТ АО ФИРМА | 151 | 124 | 82% |

Для совпавших строк:

| АпкНазначение | Строк |
|---|---:|
| СЗР | 120 |
| Удобрение | 100 |
| Пусто | 4 |

Вывод: при наличии записи номенклатуры классификация СЗР/удобрений работает корректно.

### Семена

| Контрагент | Сумма, ₽ | Строк | С найденной номенклатурой | Покрытие |
|---|---:|---:|---:|---:|
| ДГТ (Дока-Генные Технологии) ООО | 19 457 889,77 | 27 | 3 | 11% |
| СЕЛЕКЦИЯ БУДУЩЕГО ООО | 18 127 090,90 | 15 | 0 | 0% |
| НОРИКА-СЛАВИЯ ООО | 15 112 388,03 | 15 | 0 | 0% |
| ЭЙЧ-ЗЕТ-ПИ-СИ САДОКАС АО | 13 342 181,82 | 13 | 0 | 0% |

Минимум 66 млн ₽ закупок у явных семеноводческих поставщиков не классифицируется через `АпкНазначение`, поскольку соответствующие ключи отсутствуют в `raw.r1c_nomenclature`.

## Регулярное обновление ГСМ

В `dag_transform_mart.py` добавлена задача:

```text
transform_fuel_writeoff
```

Она переносит данные:

```text
staging.v_fuel_writeoff_clean
→ mart.dim_equipment
→ mart.fact_fuel_writeoff
```

Записываются:

```text
period_month
equipment_sk
fuel_brand_id
liters_start
liters_end
liters_refueled
liters_consumed
src_doc_ref
```

Защита от дублей выполняется по:

```text
src_doc_ref = doc_id-line_number
```

На момент проверки:

| Показатель | Значение |
|---|---:|
| Строк факта списания ГСМ | 572 |
| Первый месяц | 2021-07-01 |
| Последний месяц | 2026-08-01 |
| Техники | 22 |
| Фактический расход | 637 470,12 л |

## Следующие шаги

1. Найти и починить/создать регулярный загрузчик `Catalog_Номенклатура` в `raw.r1c_nomenclature`.
2. Добавить `_loaded_at` в `raw.r1c_nomenclature`.
3. После восстановления справочника повторно пересчитать ГСМ, семена, СЗР и удобрения.
4. Дополнить `dag_extract_purchases.py` полем `Договор_Key`, чтобы заполнять `dogovor_id`.
5. Разобрать крупнейших ИП-поставщиков по фактической номенклатуре и выделить семена.
6. Проверить ежедневные успешные прогоны `dag_extract_purchases` и `dag_extract_contractors`.

## Исторические затраты на обработанный гектар

Для большинства полей в 2021–2025 нет площади в `mart.v_field_area_by_season`.
Поэтому показатель «затраты на гектар площади поля» за эти годы не может быть
рассчитан достоверно только из истории полей.

Файл `40_fix_agro_input_cost_history_area.sql` изменяет
`mart.v_agro_input_cost_per_field_month` так, чтобы исторические деньги
не исключались из витрины из-за отсутствия площади. Для строк без площади
устанавливается статус:

```text
НЕТ ПЛОЩАДИ ПОЛЯ В СЕЗОНЕ
```

При этом абсолютные затраты по полю, месяцу, сезону и категории сохраняются.

Во всех строках проведённых актов списания материалов заполнено поле
`apk_ploshchad_obrabotannaya`. Площадь повторяется для нескольких строк
номенклатуры одного акта, поэтому её нельзя просто суммировать по всем
товарным строкам.

Создана отдельная витрина:

```text
mart.v_agro_input_cost_per_processed_ha_month
```

Файл:

```text
files/41_agro_input_cost_per_processed_ha.sql
```

Она считает стоимость СЗР, удобрений и семян на фактически обработанный
гектар за 2021–2026.

Правила расчёта:

1. Стоимость материалов суммируется по всем строкам акта.
2. Площадь считается один раз на документ, объект затрат, поле и категорию.
3. Для строки `ВСЕГО` площадь считается один раз на документ и поле,
   независимо от категорий — это предотвращает двойной учёт гектаров
   в актах, содержащих одновременно СЗР и удобрения.
4. Площади категорий нельзя складывать между собой как независимые гектары,
   потому что разные категории могут быть внесены на одну и ту же площадь.

Результат по всем категориям:

| Сезон | Общие затраты без НДС, ₽ | Обработано, га | Затраты, ₽/обработанный га |
|---:|---:|---:|---:|
| 2021 | 5 735 429,74 | 817,68 | 7 014,27 |
| 2022 | 26 354 536,22 | 5 496,03 | 4 795,20 |
| 2023 | 41 720 929,60 | 10 746,43 | 3 882,31 |
| 2024 | 19 249 725,76 | 1 708,50 | 11 267,03 |
| 2025 | 30 621 699,29 | 4 952,00 | 6 183,70 |
| 2026 | 54 497 232,89 | 8 267,60 | 6 591,66 |

Ограничение по семенам: SQL-модель поддерживает категорию «Семена», но
покрытие неполно из-за отсутствующих записей в `raw.r1c_nomenclature`.
На текущем срезе в витрину попала только одна строка семян 2022 года:
180 000 ₽ на 0,95 га.

## Нерешённый блок: ГСМ и работы по полю

Целевая экономика поля должна объединять:

```text
СЗР + удобрения + семена + ГСМ + работы + урожай
```

Однако `mart.fact_putevoy_rabota` не содержит заполненного `pole_id`,
а поля `toplivo_vydano` и `toplivo_vozvrat` по текущей выгрузке пусты.
Следовательно, ГСМ и работы пока нельзя достоверно отнести к конкретному
полю и включить в полную денежную себестоимость поля.

Во всех 25 696 строках `raw.r1c_putevoy_list_lines` заполнен `klyuch_svyazi`.
В следующей итерации нужно восстановить связь:

```text
путевой лист → ключ связи → задание/документ 1С с полем
```

Попытка связи с документами движения продукции требует отдельной проверки
типов ключей (`text` / `uuid`); связь с талонами взвешивания дала 0 совпадений.

## Исторические затраты на обработанный гектар

Для большинства полей в 2021–2025 нет площади в `mart.v_field_area_by_season`.
Поэтому показатель «затраты на гектар площади поля» за эти годы не может быть
рассчитан достоверно только из истории полей.

Файл `40_fix_agro_input_cost_history_area.sql` изменяет
`mart.v_agro_input_cost_per_field_month` так, чтобы исторические деньги
не исключались из витрины из-за отсутствия площади. Для строк без площади
устанавливается статус:

```text
НЕТ ПЛОЩАДИ ПОЛЯ В СЕЗОНЕ
```

При этом абсолютные затраты по полю, месяцу, сезону и категории сохраняются.

Во всех строках проведённых актов списания материалов заполнено поле
`apk_ploshchad_obrabotannaya`. Площадь повторяется для нескольких строк
номенклатуры одного акта, поэтому её нельзя просто суммировать по всем
товарным строкам.

Создана отдельная витрина:

```text
mart.v_agro_input_cost_per_processed_ha_month
```

Файл:

```text
files/41_agro_input_cost_per_processed_ha.sql
```

Она считает стоимость СЗР, удобрений и семян на фактически обработанный
гектар за 2021–2026.

Правила расчёта:

1. Стоимость материалов суммируется по всем строкам акта.
2. Площадь считается один раз на документ, объект затрат, поле и категорию.
3. Для строки `ВСЕГО` площадь считается один раз на документ и поле,
   независимо от категорий — это предотвращает двойной учёт гектаров
   в актах, содержащих одновременно СЗР и удобрения.
4. Площади категорий нельзя складывать между собой как независимые гектары,
   потому что разные категории могут быть внесены на одну и ту же площадь.

Результат по всем категориям:

| Сезон | Общие затраты без НДС, ₽ | Обработано, га | Затраты, ₽/обработанный га |
|---:|---:|---:|---:|
| 2021 | 5 735 429,74 | 817,68 | 7 014,27 |
| 2022 | 26 354 536,22 | 5 496,03 | 4 795,20 |
| 2023 | 41 720 929,60 | 10 746,43 | 3 882,31 |
| 2024 | 19 249 725,76 | 1 708,50 | 11 267,03 |
| 2025 | 30 621 699,29 | 4 952,00 | 6 183,70 |
| 2026 | 54 497 232,89 | 8 267,60 | 6 591,66 |

Ограничение по семенам: SQL-модель поддерживает категорию «Семена», но
покрытие неполно из-за отсутствующих записей в `raw.r1c_nomenclature`.
На текущем срезе в витрину попала только одна строка семян 2022 года:
180 000 ₽ на 0,95 га.

## Нерешённый блок: ГСМ и работы по полю

Целевая экономика поля должна объединять:

```text
СЗР + удобрения + семена + ГСМ + работы + урожай
```

Однако `mart.fact_putevoy_rabota` не содержит заполненного `pole_id`,
а поля `toplivo_vydano` и `toplivo_vozvrat` по текущей выгрузке пусты.
Следовательно, ГСМ и работы пока нельзя достоверно отнести к конкретному
полю и включить в полную денежную себестоимость поля.

Во всех 25 696 строках `raw.r1c_putevoy_list_lines` заполнен `klyuch_svyazi`.
В следующей итерации нужно восстановить связь:

```text
путевой лист → ключ связи → задание/документ 1С с полем
```

Попытка связи с документами движения продукции требует отдельной проверки
типов ключей (`text` / `uuid`); связь с талонами взвешивания дала 0 совпадений.

## Исторические затраты на обработанный гектар

Для большинства полей в 2021–2025 нет площади в `mart.v_field_area_by_season`.
Поэтому показатель «затраты на гектар площади поля» за эти годы не может быть
рассчитан достоверно только из истории полей.

Файл `40_fix_agro_input_cost_history_area.sql` изменяет
`mart.v_agro_input_cost_per_field_month` так, чтобы исторические деньги
не исключались из витрины из-за отсутствия площади. Для строк без площади
устанавливается статус:

```text
НЕТ ПЛОЩАДИ ПОЛЯ В СЕЗОНЕ
```

При этом абсолютные затраты по полю, месяцу, сезону и категории сохраняются.

Во всех строках проведённых актов списания материалов заполнено поле
`apk_ploshchad_obrabotannaya`. Площадь повторяется для нескольких строк
номенклатуры одного акта, поэтому её нельзя просто суммировать по всем
товарным строкам.

Создана отдельная витрина:

```text
mart.v_agro_input_cost_per_processed_ha_month
```

Файл:

```text
files/41_agro_input_cost_per_processed_ha.sql
```

Она считает стоимость СЗР, удобрений и семян на фактически обработанный
гектар за 2021–2026.

Правила расчёта:

1. Стоимость материалов суммируется по всем строкам акта.
2. Площадь считается один раз на документ, объект затрат, поле и категорию.
3. Для строки `ВСЕГО` площадь считается один раз на документ и поле,
   независимо от категорий — это предотвращает двойной учёт гектаров
   в актах, содержащих одновременно СЗР и удобрения.
4. Площади категорий нельзя складывать между собой как независимые гектары,
   потому что разные категории могут быть внесены на одну и ту же площадь.

Результат по всем категориям:

| Сезон | Общие затраты без НДС, ₽ | Обработано, га | Затраты, ₽/обработанный га |
|---:|---:|---:|---:|
| 2021 | 5 735 429,74 | 817,68 | 7 014,27 |
| 2022 | 26 354 536,22 | 5 496,03 | 4 795,20 |
| 2023 | 41 720 929,60 | 10 746,43 | 3 882,31 |
| 2024 | 19 249 725,76 | 1 708,50 | 11 267,03 |
| 2025 | 30 621 699,29 | 4 952,00 | 6 183,70 |
| 2026 | 54 497 232,89 | 8 267,60 | 6 591,66 |

Ограничение по семенам: SQL-модель поддерживает категорию «Семена», но
покрытие неполно из-за отсутствующих записей в `raw.r1c_nomenclature`.
На текущем срезе в витрину попала только одна строка семян 2022 года:
180 000 ₽ на 0,95 га.

## Нерешённый блок: ГСМ и работы по полю

Целевая экономика поля должна объединять:

```text
СЗР + удобрения + семена + ГСМ + работы + урожай
```

Однако `mart.fact_putevoy_rabota` не содержит заполненного `pole_id`,
а поля `toplivo_vydano` и `toplivo_vozvrat` по текущей выгрузке пусты.
Следовательно, ГСМ и работы пока нельзя достоверно отнести к конкретному
полю и включить в полную денежную себестоимость поля.

Во всех 25 696 строках `raw.r1c_putevoy_list_lines` заполнен `klyuch_svyazi`.
В следующей итерации нужно восстановить связь:

```text
путевой лист → ключ связи → задание/документ 1С с полем
```

Попытка связи с документами движения продукции требует отдельной проверки
типов ключей (`text` / `uuid`); связь с талонами взвешивания дала 0 совпадений.
