# BI 1C / Лесные дали — аудит данных и OData

**Дата сессии:** 15 августа 2026

## Цель

Зафиксировать фактическую готовность данных 1С для BI/DataLens, результаты аудита RAW → staging → mart, созданные представления движения ТМЦ и доступность OData-данных для агро-KPI.

## Созданные представления

### `staging.v_dvizhenie_produkcii_lines_clean`

View соединяет заголовки `staging.v_dvizhenie_produkcii_clean` и строки `raw.r1c_dvizhenie_produkcii_lines`.

Содержит документ, строку, дату, операцию, номенклатуру, количество, сумму, серию, склады/контрагентов, объект затрат, тип работ, очищенные UUID поля/единицы и флаги `_deletionmark`, `_posted`.

Единое правило активного набора:

```sql
COALESCE(_deletionmark, FALSE) = FALSE
AND COALESCE(_posted, FALSE) = TRUE
```

### `mart.v_tmc_movement`

View оставляет только активные строки движения и добавляет:

- бизнес-тип движения: `movement_type`;
- знак и знаковое количество: `quantity_sign`, `quantity_signed`;
- номенклатуру и fallback `[Не загружено из 1С] <UUID>`;
- `is_nomenclature_resolved`;
- единицу через `raw.r1c_upakovki`;
- `uom_code`, `uom_name`, `uom_weight_kg`, `uom_volume_l`;
- склад отправителя/получателя, доступную связь с полем и объектом затрат.

| Операция 1С | Тип движения | Знак |
|---|---|---:|
| `ПередачаМатериаловВПроизводство` | `material_issue_to_production` | -1 |
| `ПередачаПродукцииИзПроизводства` | `production_receipt` | +1 |
| `ПередачаМатериаловВКладовую` | `material_return_to_warehouse` | +1 |
| `ПередачаПродукцииИзКладовой` | `product_issue_from_warehouse` | -1 |

## Движение продукции и материалов

- Всего строк движения: **10 599**.
- Активных строк: **10 572**.
- Исключено: **27** строк.
- Одновременно удалены и непроведены: **24** строки.
- Всего документов: **7 403**.
- Активных документов: **7 377**.
- Дубликатов `_id` документов и строк: **0**.

| Тип движения | Строк | Номенклатура найдена | Номенклатура не найдена | Единица найдена |
|---|---:|---:|---:|---:|
| `material_return_to_warehouse` | 7 796 | 4 395 | 3 401 | 4 395 |
| `production_receipt` | 2 119 | 83 | 2 036 | 83 |
| `product_issue_from_warehouse` | 627 | 476 | 151 | 476 |
| `material_issue_to_production` | 30 | 29 | 1 | 29 |
| **Итого** | **10 572** | **4 983** | **5 589** | **4 983** |

## Номенклатура и единицы

- `raw.r1c_nomenclature`: **4 717** строк.
- `raw.r1c_nomenclature_backup_20260815`: **15 846** строк.
- В движениях используются 344 UUID номенклатуры; 89 UUID отсутствуют в текущем справочнике.
- 5 589 строк движения не имеют расшифровки номенклатуры.
- Наиболее частый отсутствующий UUID: `13d3b0a9-30ed-11ec-b9aa-00505689914b`, 1 850 строк движения.
- `raw.r1c_uom` пуста; текущие используемые UUID единиц полностью разрешаются через `raw.r1c_upakovki`.

| Единица | Код | Строк | Количество |
|---|---:|---:|---:|
| Не определена | — | 5 589 | 36 168 345.269 |
| кг | 166 | 1 783 | 91 003 007.822 |
| л | 112 | 2 893 | 49 857.021 |
| т | 168 | 215 | 1 130.850 |
| га | 059 | 82 | 386.916 |
| шт | 796 | 10 | 10.000 |

Нельзя суммировать `quantity` между разными единицами. В DataLens использовать количество только вместе с `uom_name`.

Во всех активных строках движения `summa = 0.00`. До появления стоимостного источника нельзя рассчитывать стоимость, себестоимость, цены, стоимость запасов и финансовые KPI.

## Инвентаризация слоёв

### Наполненные ключевые RAW-объекты

| Объект | Строк |
|---|---:|
| `raw.r1c_dvizhenie_produkcii` | 7 403 |
| `raw.r1c_dvizhenie_produkcii_lines` | 10 599 |
| `raw.r1c_putevoy_list` | 12 239 |
| `raw.r1c_putevoy_list_lines` | 25 285 |
| `raw.r1c_nomenclature` | 4 717 |
| `raw.r1c_nomenclature_backup_20260815` | 15 846 |
| `raw.r1c_polya` | 48 |
| `raw.r1c_upakovki` | 143 |
| `raw.r1c_equipment` | 169 |
| `raw.r1c_warehouses` | 925 |
| `raw.r1c_crops` | 165 |
| `raw.r1c_employees` | 757 |

### Пустые RAW-объекты

```text
r1c_crop_rotations
r1c_equipment_models
r1c_farms
r1c_fields
r1c_field_writeoff_doc
r1c_field_writeoff_lines
r1c_harvest_doc
r1c_harvest_lines
r1c_nomen_characteristics
r1c_tech_operations
r1c_uom
r1c_warehouse_rooms
r1c_waybill
```

### Staging и mart

| Объект | Строк | Статус |
|---|---:|---|
| `staging.v_dvizhenie_produkcii_clean` | 7 403 | Наполнена |
| `staging.v_dvizhenie_produkcii_lines_clean` | 10 599 | Наполнена |
| `mart.v_tmc_movement` | 10 572 | Готова с ограничениями |
| `mart.fact_putevoy_rabota` | 25 279 | Наполнен с ограничениями |
| `mart.v_putevoy_rabota_bi` | 25 279 | Наполнена |
| `mart.fact_vypusk_urozhaya` | 2 119 | Частично применим |
| `mart.v_vypusk_urozhaya_bi` | 2 119 | Частично применима |
| `mart.fact_spisanie_materialov` | 30 | Частично применим |
| `mart.v_spisanie_materialov_bi` | 30 | Частично применима |

Пустые mart-объекты:

```text
dim_crop
dim_downtime_reason
dim_shift
fact_downtime
fact_field_costs
fact_fuel_usage
fact_harvest
fact_production_output
fact_storage_balance
fact_tmc_usage
```

## Аудит фактов и измерений

### Путевые листы

`mart.fact_putevoy_rabota` содержит 25 279 строк.

- Связь с `dim_equipment` — 100%, 24 используемые единицы техники, сирот нет.
- `obem_rabot_ga`: сумма 65 278.8670; нулевой объём в 17 748 строках. Для фактически выполненных работ применять фильтр `obem_rabot_ga > 0`.
- `narabotka_moto_chas`: ненулевых значений только 7, сумма 7.00.
- `probeg_km`, `toplivo_vydano`, `toplivo_vozvrat`: NULL во всех строках.
- `norma_vyrabotki`: сумма 339 187.190; это норматив, а не фактический объём.
- `pole_id` содержит нулевой GUID во всех 25 279 строках.

### Списание и выпуск

`mart.fact_spisanie_materialov`:

- 30 строк, сумма количества 639 502.055;
- `summa = 0.00` во всех строках;
- номенклатура разрешается в 29 строках;
- `edinica_id` пуст и `pole_id` нулевой GUID во всех строках.

`mart.fact_vypusk_urozhaya`:

- 2 119 строк, сумма количества 5 712 216.974;
- `summa = 0.00` во всех строках;
- номенклатура разрешается только в 83 строках, 2 036 строк не находят `dim_nomenclature`;
- `edinica_id` пуст и `pole_id` нулевой GUID во всех строках.

### Измерения

| Измерение | Строк | Качество | Ограничение |
|---|---:|---|---|
| `mart.dim_equipment` | 169 | Ключи, имена и активность заполнены; дублей нет | Готово |
| `mart.dim_field` | 48 | Ключи, имена и организация заполнены; дублей нет | `area_ha` NULL во всех строках |
| `mart.dim_nomenclature` | 4 721 | Ключи и имена заполнены; дублей нет | `pack_size_kg` NULL во всех строках; 2 пустых `code_1c` |

Нулевой GUID поля во всех старых фактах:

```text
00000000-0000-0000-0000-000000000000
```

Поэтому старые факты нельзя применять для аналитики по полям, урожайности, затратам на гектар и нормам на гектар.

## Актуальность и целостность

- Период движения продукции: 1 июля 2021 — 31 июля 2026.
- Период путевых листов: 1 июля 2021 — 31 июля 2026.
- `raw.r1c_dvizhenie_produkcii` обновлён 15 августа 2026, 19:00 MSK.
- `raw.r1c_putevoy_list` обновлён 13 августа 2026, 16:46 MSK.
- В движении: 23 удалённых и 26 непроведённых документов.
- В путевых листах: 0 удалённых и 3 непроведённых документов.
- Дубликаты документов и строк движения/путевых листов: не найдены.

## OData: поля, площади и культуры

`$metadata` OData возвращает HTTP 500, поэтому сущности проверялись прямыми запросами.

| EntitySet | HTTP | Назначение |
|---|---:|---|
| `Catalog_АпкПоля` | 200 | Справочник полей |
| `Catalog_АпкПоля_ИсторияПоля` | 200 | История площадей, культур и объектов затрат |
| `Catalog_АпкПоля_ИсторияИспользования` | 200 | Использование поля с года |
| `Catalog_АпкПоля_ИсторияПринадлежности` | 200 | Принадлежность подразделению по годам |
| `Catalog_АпкПоля_КадастровыеУчастки` | 200 | Кадастровые контуры и площади |

Табличные части 1С нельзя читать через `$expand`: запросы с `$expand=ИсторияПоля`, `ИсторияИспользования`, `КадастровыеУчастки` дали HTTP 501. Они доступны как отдельные EntitySet.

### `Catalog_АпкПоля_ИсторияПоля`

Подтверждённые поля:

```text
Ref_Key
LineNumber
ГодУрожая
Культура
ПлощадьОбщая
ПлощадьСева
ПлощадьКонтура
ОбъектЗатратТекущегоГода_Key
НачалоПериодаАктуальности
КонецПериодаАктуальности
НеИспользуется
Подразделение_Key
```

Результаты проверки:

- 747 строк истории;
- 48 полей;
- 614 уникальных объектов затрат;
- 151 культура;
- 640 строк с ненулевой `ПлощадьСева`;
- `ГодУрожая`: 2020 — 19, 2021 — 13, 2022 — 38, 2023 — 40, 2024 — 35, 2025 — 45, 2026 — 47, значение 0 — 510.

`ГодУрожая = 0` не считать автоматически отсутствием данных: использовать также период актуальности.

## Почему урожайность пока не считается

У активных документов `ПередачаПродукцииИзПроизводства`:

- 1 868 документов;
- заполненный `cost_object_id`: 0;
- заполненный `work_type_id`: 0;
- комментарий есть только в одном документе (`Выпуск для тестов`);
- 117 складов-источников и 52 получателя.

Следовательно, выпуск нельзя достоверно связать с полем через склад, получателя или комментарий.

Следующие предполагаемые документы не опубликованы в OData и возвращают HTTP 404:

```text
Document_ОприходованиеУрожая
Document_ОприходованиеПродукции
Document_АпкОприходованиеУрожая
Document_ОтчетПроизводстваЗаСмену
Document_АпкОтчетПроизводстваЗаСмену
Document_АпкУборкаУрожая
Document_УборкаУрожая
```

Корректная будущая модель:

```text
Выпуск продукции
  → поле или объект затрат
  → Catalog_АпкПоля_ИсторияПоля
  → поле + культура + площадь сева
  → урожайность
```

Формула при выпуске в кг:

```text
урожайность, т/га = количество_кг / (1000 × площадь_сева_га)
```

## DataLens: что можно делать сейчас

Подключать:

```sql
mart.v_tmc_movement
mart.v_putevoy_rabota_bi
```

Безопасные отчёты:

1. Движение ТМЦ по датам, операциям, складам, номенклатуре и единицам.
2. Топ номенклатуры с фильтрами:

```sql
is_nomenclature_resolved = TRUE
uom_name <> '[Не определена]'
```

3. Работы техники по датам/технике с фильтром:

```sql
obem_rabot_ga > 0
```

4. Мониторинг качества: доля нераспознанной номенклатуры и строк без единицы.

Не использовать до исправлений:

- урожайность по полям;
- KPI на гектар;
- стоимость, себестоимость, цены и финансовые KPI;
- складские остатки как готовый баланс;
- расход топлива, пробег и моточасы;
- пустые fact-таблицы;
- общий итог количества без разреза по единице.

## Приоритетный план

1. Проверить и восстановить `raw.r1c_nomenclature` из `raw.r1c_nomenclature_backup_20260815` после проверки структуры и дедупликации.
2. Добавить/исправить DAG для `Catalog_Номенклатура` и логировать фактическое количество строк после upsert.
3. Загрузить в RAW `Catalog_АпкПоля_ИсторияПоля`, `ИсторияИспользования`, `ИсторияПринадлежности`, `КадастровыеУчастки`; создать историческую витрину площадей и культур.
4. Опубликовать в OData документ или регистр с выпуском урожая и `Поле_Key` либо `ОбъектЗатрат_Key`; альтернативно обеспечить заполнение `cost_object_id` в документе выпуска.
5. После появления надёжной связи построить урожайность по полю, культуре и году.
6. Доработать путевые листы: поле, пробег, топливо и моточасы.
7. Загрузить отсутствующие контуры: списания на поле, уборка, топливо, простои, накладные.

## Технические заметки

- Рабочая локальная папка: `/Users/sexyidol2003/Desktop/ЛД БД/LesDaliBI/LesDaliBI/files`.
- PostgreSQL DWH: контейнер `ldali-postgres-dwh`.
- Airflow web: контейнер `ldali-airflow-web`.
- `ldali-airflow-webserver` не существует.
- Основные DAG: `dag_extract_catalogs.py`, `dag_extract_catalogs2.py`, `dag_extract_dvizhenie.py`, `dag_extract_putevoy_list.py`, `dag_transform_mart.py`.
- `dag_extract_catalogs.py` грузит `Catalog_УпаковкиЕдиницыИзмерения` в `raw.r1c_upakovki` и `Catalog_АпкПоля` в `raw.r1c_polya`, но пока не загружает историю поля; по этой причине `dim_field.area_ha` остаётся NULL.

## Итог сессии

DataLens можно подключать для ограниченных количественных отчётов по движению ТМЦ и работам техники. Урожайность, показатели на гектар, топливо, стоимость и полные агро-KPI откладываются до загрузки истории полей и появления корректной связи выпуска с полем/объектом затрат.
