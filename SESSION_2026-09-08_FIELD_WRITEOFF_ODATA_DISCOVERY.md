# Находка: OData-источник для "Акт списания семян, удобрений, ядов"

Дата фиксации: 2026-09-08
Репозиторий: https://github.com/SexyIdol2003/LesDaliBI
Связано с raw-таблицами: `raw.r1c_field_writeoff_doc`, `raw.r1c_field_writeoff_lines`

## Проблема

Прямые попытки обратиться к OData-сущности по ожидаемым именам документа
(`АктСписанияСеменУдобренийИЯдов`, `АктСписанияСемянУдобренийИЯдов`,
`АпкСписаниеСеменУдобренийИЯдов`, `СписаниеСеменУдобренийИЯдов`,
`АпкАктСписанияСеменУдобренийИЯдов`) давали `404 Not Found`.
Корневой `$metadata` документ REST-сервиса при этом стабильно падал с
`odata.error code -1` ("внутренняя ошибка OData сервиса"), что не позволяло
получить список сущностей напрямую.

## Причина

В конфигурации 1С:ERP Агропромышленный комплекс документ, который пользователь
видит как «Акт списания семян, удобрений, ядов», физически реализован как
универсальный документ «Движение продукции и материалов»
(`Документ.ДвижениеПродукцииИМатериалов`), общий для множества видов
хозяйственных операций (передача в кладовую, передача в производство,
списание и т.д.). Конкретное "лицо" документа для пользователя определяется
не типом объекта, а значением поля-классификатора:

```
АпкВидДокумента = "АктНаСписанияСемянУдобренийИЯдов"
```

## Как получить технически верное имя объекта (в общем случае)

1. Открыть нужный документ в клиенте 1С (веб или толстый).
2. В форме документа выбрать «Все действия» → «Изменить форму...».
3. В заголовке / корне дерева открывшегося конструктора форм отображается
   путь вида `Документ.ИмяДокумента.Форма.ФормаДокумента` — это и есть
   техническое имя.
4. Права администратора/конфигуратора для этого способа не требуются.

(Альтернатива — Конфигуратор → дерево конфигурации → Документы, но требует
роль с полным доступом к метаданным, которой у обычных пользователей может
не быть.)

## Рабочий OData-запрос

Базовый URL сущности:
```
GET /ld_erp_ibiOdata/odata/standard.odata/Document_ДвижениеПродукцииИМатериалов
```

Фильтр только на акты списания семян/удобрений/ядов:
```
$filter=АпкВидДокумента eq 'АктНаСписанияСемянУдобренийИЯдов'
```

Полный пример (curl, через Docker/Airflow):
```bash
curl -s -u 'OdataBi:xxx123' \
  "http://10.50.254.22/ld_erp_ibiOdata/odata/standard.odata/Document_ДвижениеПродукцииИМатериалов?\$format=json&\$filter=АпкВидДокумента%20eq%20'АктНаСписанияСемянУдобренийИЯдов'&\$top=1000"
```

Проверено: запрос возвращает корректный набор документов, все с нужным
значением `АпкВидДокумента`, без посторонних видов операций.

## Ключевые поля в ответе

Заголовок документа:
- `Ref_Key`, `Number`, `Date`, `Posted`, `Статус`
- `Организация_Key`, `Отправитель` / `Получатель` (склады, `Type: Catalog_Склады`)
- `ХозяйственнаяОперация` (в нашем кейсе — `ПередачаМатериаловВКладовую`)
- `АпкВидДокумента` — классификатор представления документа (см. выше)
- `АпкВидРаботы_Key`, `АпкОбъектЗатрат_Key` (поле/объект затрат)

Табличная часть `Товары` (агроаналитика):
- `Номенклатура_Key`, `Характеристика_Key`
- `Количество`, `КоличествоУпаковок`
- `АпкПлощадьОбработанная` (Га)
- `АпкРасходНаГа` (норма расхода на гектар)
- `ГруппаПродукции_Key`

## Что нужно сделать дальше для DWH

1. Убрать/увеличить `$top` и добавить `$skip` для полной постраничной выгрузки
   (стандартный OData-сервис 1С ограничивает объём одного ответа).
2. Добавить инкрементальный фильтр по дате в DAG-загрузчик, например:
   `$filter=АпкВидДокумента eq 'АктНаСписанияСемянУдобренийИЯдов' and Date ge datetime'YYYY-MM-DDT00:00:00'`.
3. Разыменовать справочные ключи (`Номенклатура_Key`, `АпкВидРаботы_Key`,
   `АпкОбъектЗатрат_Key`, `Организация_Key`, склады) отдельными запросами к
   соответствующим `Catalog_*`, либо через `$expand`, если поддерживается.
4. Замапить это на `raw.r1c_field_writeoff_doc` (заголовок) и
   `raw.r1c_field_writeoff_lines` (табличная часть `Товары`) в существующей
   схеме RAW-слоя.
5. Держать в уме: другие "виды" документов в конфигурации АПК, вероятно,
   организованы по такому же принципу (один физический документ +
   классификатор `АпкВидДокумента`/аналогичное поле) — при поиске следующих
   источников сразу проверять это предположение, не тратя время на перебор
   несуществующих имён сущностей.
